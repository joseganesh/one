<?xml version='1.0' encoding='utf-8'?>
<!--
  ~ Hibernate, Relational Persistence for Idiomatic Java
  ~
  ~ License: GNU Lesser General Public License (LGPL), version 2.1 or later.
  ~ See the lgpl.txt file in the root directory or <http://www.gnu.org/licenses/lgpl-2.1.html>.
  -->
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost/jewellery</property>
        <property name="connection.username">root</property>
        <property name="connection.password"></property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Names the annotated entity class -->
        <mapping class="org.jewellery.model.ProductModel"/>
        <mapping class="org.jewellery.model.AccountGroupModel"/>
        <mapping class="org.jewellery.model.CashDataModel"/>
        <mapping class="org.jewellery.model.CategoryModel"/>
        <mapping class="org.jewellery.model.CustomerModel"/>
        <mapping class="org.jewellery.model.ModelJewel"/>
        <mapping class="org.jewellery.model.ProductModel"/>
        <mapping class="org.jewellery.model.RateModel"/>
        <mapping class="org.jewellery.model.SalesScreenModel"/>
        <mapping class="org.jewellery.model.StockModel"/>
        <mapping class="org.jewellery.model.TaxModel"/>
        <mapping class="org.jewellery.model.UnitModel"/>
        <mapping class="org.jewellery.model.VendorModel"/>

    </session-factory>

</hibernate-configuration>